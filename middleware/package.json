{
  "name": "softrd_js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build_core": "cmake --build ../core/build  --config Release -v -j8 || cmake --install ../core/build",
    "cmake": "cmake-js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.5.0",
    "cmake-js": "^6.1.0",
    "node-addon-api": "^1.7.1"
  }
}