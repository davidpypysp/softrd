import React from "react";
import { Card } from "@blueprintjs/core";

export default class Header extends React.Component {
    render() {
        return <Card className="header">Help</Card>;
    }
}
